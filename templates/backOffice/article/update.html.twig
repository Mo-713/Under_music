{% extends 'base.html.twig' %}

{% block title %}
	Update
{% endblock %}

{% block main %}

	<h1>Modif</h1>
	{{form_start(form)}}
	{{form_row(form.title)}}
	{# {{form_row(form.content)}} #}
	<button>{{ labelBtn|default('Save') }}</button>
	{{form_end(form)}}

{% endblock %}
